const cover=document.getElementById("cover");const inside=document.getElementById("inside");const back=document.getElementById("backBtn");function openInvite(){cover.classList.remove("visible");inside.classList.add("visible");back.style.display="block";}function closeInvite(){inside.classList.remove("visible");cover.classList.add("visible");back.style.display="none";}cover.addEventListener("click",openInvite);setTimeout(()=>{if(cover.classList.contains("visible"))openInvite();},5000);back.addEventListener("click",closeInvite);